import styled from "styled-components"

const StyledSelectContainer = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
`

const StyledSelect = styled.select`
  padding: 0.5rem;
  font-size: 1rem;
`
export { StyledSelect, StyledSelectContainer }
